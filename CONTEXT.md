# 拾学领域词汇表

本文只定义产品语言。代码、测试、界面与未来“小拾”的能力协议必须使用同一含义，不能另造同义词。

| 术语 | 定义 | 不代表 |
| --- | --- | --- |
| 工作区状态（`WorkspaceState`） | 拾学在本地持久化的完整版本化业务快照。v3 包含清单、任务、重复系列、发生项、提醒、学习记录和事件。 | UI 临时状态、同步游标、密钥。 |
| 清单分组（`ListGroup`） | 仅用于排列清单的一层容器。 | 可无限嵌套的文件夹。 |
| 清单（`TaskList`） | 任务的唯一主归属；学习清单可以附带目标、验收标准和周目标。 | 跨清单分类标签。 |
| 分区（`ListSection`） | 清单内的一层可排序分段。 | 子清单。 |
| 标签（`Tag`） | 可跨清单附着到任务的正交分类。 | 任务的主归属。 |
| 任务（`Task`） | 可一次完成的工作定义，保存标题、说明、优先级、清单、标签及可选学习属性。 | 重复日程中的某一次。 |
| 学习模式（`TaskMode: learning`） | 开启学习证据、专注记录和复习连接的任务能力。现有 v2 任务迁移后默认开启。 | 独立于通用任务的第二套模型。 |
| 计划时间（`TaskSchedule`） | 任务打算开始的本地日期/时间与预计时长；有开始时间和时长时形成日历时间块。 | 必须完成的最后期限。 |
| 截止时间（`TaskDeadline`） | 独立于计划时间的完成期限。 | 日历时间块的结束时间。 |
| 重复系列（`RecurrenceSeries`） | 规则、锚点、结束条件和未来生成窗口的稳定定义。 | 已完成历史。 |
| 发生项（`TaskOccurrence`） | 重复系列在某个计划时间点的一次独立执行，可完成、跳过或单独改期。 | 通过修改同一任务日期模拟的“下一次”。 |
| 提醒规则（`ReminderRule`） | 绑定任务或发生项的提醒意图，例如开始时、提前 10 分钟或绝对时间。 | 已经投递的通知。 |
| 提醒投递（`ReminderDelivery`） | 某条提醒在某次发生项上的可去重投递记录，含触发、稍后提醒和处理状态。 | 提醒配置本身。 |
| 今天（`Today`） | 计划在今天、今天截止、已逾期或今天发生的重复项，按来源分组展示。 | 仅按一个日期字段筛选。 |
| 逾期（`Overdue`） | 截止时间早于当前时间且未完成/取消；原截止事实不可因加入今天而覆盖。 | 自动改期。 |
| 复习任务连接（`ReviewTaskLink`） | 学习复习记录与一个可见任务/发生项之间的双向关系；完成任务会回写复习结果。 | 隐藏在 Today/日历之外的后台提醒。 |
| 能力命令（`CapabilityCommand`） | 通过统一领域服务产生状态变化的版本化请求。人类 UI 与未来 Agent 使用同一入口。 | 模型工具调用或对话消息。 |
| 能力查询（`CapabilityQuery`） | 不改变工作区状态的版本化读取请求。 | 允许绕过权限的内部访问。 |
| 预演（`preview`） | 在不写入的情况下校验命令并返回影响范围、风险、可逆性与差异摘要。 | 对真实状态的隐式保存。 |
| 撤销令牌（`UndoToken`） | 成功执行可逆命令后返回的短期、一次性补偿操作凭据。 | 任意历史版本恢复。 |

## 边界规则

- 任务只归属一个清单，可带多个标签；层级固定为“清单分组 → 清单 → 分区”。
- 一次性任务没有发生项；重复任务的每次完成、跳过和改期都落在发生项，系列保留规则。
- 计划时间和截止时间可独立存在；仅有截止日期不会自动占据日历时间槽。
- 关闭学习模式不删除既有学习记录，只停止要求新的学习字段。
- Agent、快捷键、通知按钮和可见 UI 都只能调用能力查询/命令，不直接改写持久化快照。
