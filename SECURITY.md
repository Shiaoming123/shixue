# 安全政策

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| 0.2.x | ✅ 支持中 |
| 0.1.x | ❌ 不再支持 |

## 报告漏洞

**请勿通过公开 Issue 报告安全漏洞。**

请通过以下私下渠道报告：

- GitHub 仓库的 **Security → Report a vulnerability**（私有漏洞上报），或
- 通过维护者 GitHub 主页提供的私密联系方式联系。

收到报告后，我们会尽快确认、修复，并在修复后通过安全公告披露。请尽量提供可复现步骤与影响评估，以便我们优先处理。

## 关于更新签名密钥

`TAURI_SIGNING_PRIVATE_KEY` 用于给自动更新包签名，仅应存放于仓库 Secrets 与本地 `~/.tauri/`，**绝不应提交进代码库**。

如发现密钥泄露：

1. 立即轮换密钥（重新生成密钥对并替换 `tauri.conf.json` 中的 `pubkey`）。
2. 用新密钥重新签名并发布受影响的版本，使已安装用户能校验到合法更新。
3. 在公告中说明轮换原因。
