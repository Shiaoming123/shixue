# v0.3.0 Acceptance Ledger

Updated: 2026-09-07 (Asia/Shanghai)

PR #25 merged the release guard, legacy WorkspaceState v3 compatibility repair,
and corrected Windows package-smoke data boundary into `main` at `bcd174c`. A
local unsigned Windows acceptance candidate was rebuilt from that commit. The
official release identity and public assets remain the exact `v0.3.0` tag and
its tag-workflow output; no local artifact is presented as a signed release.

The earlier Windows artifacts built from `893c9bb` are superseded. Their former
smoke report incorrectly treated child `APPDATA` and `LOCALAPPDATA` overrides as
Tauri data isolation even though Tauri loaded the real Windows Known Folder
database.

Status meanings:

- `PASS`: the listed command or observation completed against the stated source
  or exact artifact bytes.
- `FAIL`: the command ran but its acceptance condition did not pass.
- `NOT_RUN`: the listed manual or native observation has not been performed.
- `BLOCKED`: an external identity, environment, device, or reputation result is
  unavailable.

## Source gates

| Gate | Status | Evidence |
| --- | --- | --- |
| Version alignment | `PASS` | `package.json`, npm lockfile, Cargo manifest/lockfile, and Tauri config all resolve to `0.3.0`. |
| `npm test` | `PASS` | 718/718 tests passed, with zero failures or skipped tests. |
| `npm run release:check -- --mode=release` | `PASS` | Rerun at `bcd174c`: 38 release-suite files, 353 passed, 0 failed, 0 skipped; all 10 command gates passed. |
| `npm run verify` | `PASS` | Full tests, typecheck, desktop/Web builds, layout check, and documentation check passed. |
| `npm run rust:verify` | `PASS` | Rust build/check and 13 tests passed; no test was ignored. The MSVC linker emitted one informational import-library warning. |
| GitHub CI for PR #25 | `PASS` | Both triggered CI runs passed Windows npm diagnostics, test/typecheck/build, and Cargo checks; Vercel also passed. |
| Release workflow full-suite guard | `PASS` | The tag workflow runs `npm test` before strict release validation; the release-kit contract fails if that step is removed. |
| Legacy v3 compatibility | `PASS` | The observed revision-82 shape, including a missing legacy `completion` field, repairs only the deleted pending review target. SQLite and IndexedDB tests prove backup-before-replacement, failure atomicity, idempotence, and rejection of unknown damage. |
| Windows smoke data boundary | `PASS` | Tests and the final local smoke resolve Windows Known Folders, require exact absent identity directories, atomically claim both directories, and clean only directories owned by the run. |
| `npm run release:provenance` | `NOT_RUN` | Provenance will be generated and verified for the clean `v0.3.0` tag. |

## Windows package and installed-app acceptance

Local candidate source: `bcd174ca98a23f9759b8bf6af75162fa858eee6f`.

| Artifact | Bytes | SHA-256 | Authenticode |
| --- | ---: | --- | --- |
| `Shixue_0.3.0_x64_Setup.exe` | 9,448,545 | `6d2cd149752840ae682d76d65ed143be0b7aadb31d5d3bb07b023eb5a9e26939` | `NotSigned` |
| `Shixue_0.3.0_x64_Installer.msi` | 11,378,688 | `68aabe3cd29647e3b2b21b8344d2beb9359d50240a5ac28c47141f1127fa2e53` | `NotSigned` |
| `Shixue_0.3.0_x64_Portable.exe` | 22,832,640 | `70fdc265ba58f11fe714bcf97862e7afa84fc60b7676c89c07712bce74dce594` | `NotSigned` |

| Check | Status | Evidence or boundary |
| --- | --- | --- |
| Build and audit Portable, NSIS, MSI, hashes, and manifest | `PASS` | `npm run package:windows` completed and the manifest hashes match the exact local files above. |
| Install, launch, relaunch, and uninstall NSIS | `PASS` | The manifest-selected NSIS installed into the owned smoke directory, stayed alive twice, uninstalled, removed its registry identity, and left no owned residue. |
| Product-data protection | `PASS` | Both exact application data directories were atomically renamed on the same disk before smoke and restored in `finally`; the real `study.db` SHA-256 remained `C6EF465C30FD98CA5C5CAD30B94B07EF23F6803D613F8ADE74C014E928AF3FDF`. |
| Fresh packaged workspace | `PASS` | The exact Portable candidate opened with truly absent identity directories and exposed the current main navigation, global search, settings, and current navigation copy without a storage error. |
| Existing revision-82 packaged workspace | `PASS` | The exact Portable candidate opened a disposable copy, created a v3 repair backup, restored the pending review target, retained its pending link, and showed no storage error. Normal startup saves advanced the copy beyond revision 83. |
| Full seven-destination and calendar-mode walkthrough | `NOT_RUN` | Automated inspection proved the current shell and representative entry points; it did not manually traverse every destination and calendar mode. |
| Retain appearance across restart | `NOT_RUN` | Requires a manual interaction in the exact packaged app. |
| Global quick-add shortcut | `NOT_RUN` | Requires a manual system shortcut observation. |
| Two reminders, delivery, snooze, and completion | `NOT_RUN` | Requires timed native interaction. Native notification action buttons remain `UNSUPPORTED`. |
| Tray hide, reopen, quit, and no post-quit delivery | `NOT_RUN` | Requires manual tray and timed delivery observation. |
| MSI installation | `NOT_RUN` | The final MSI bytes were audited but not installed. |
| Windows display scaling at 200% | `NOT_RUN` | Requires a manual display-setting change and walkthrough. |

## Trust, updater, accessibility, and mobile

| Area | Status | Boundary |
| --- | --- | --- |
| Authenticode signing | `BLOCKED` | No certificate-backed publisher identity is available; the local artifacts are explicitly unsigned. |
| Updater end-to-end | `BLOCKED` | Configuration exists, but there is not yet a hosted v0.3.0 updater payload or installed-client update observation. |
| SmartScreen | `BLOCKED` | Requires downloaded public bytes, valid publisher signing, and real reputation evidence. |
| Windows Narrator | `NOT_RUN` | Keyboard and screen-reader behavior requires manual validation in the installed app. |
| Android/iOS native acceptance | `NOT_RUN` | No current v0.3.0 APK, AAB, IPA, simulator, or real-device run is recorded. Responsive Web evidence is not native evidence. |
| Mobile signing and store delivery | `BLOCKED` | Platform credentials, signed release artifacts, real-device checks, and store submission evidence are unavailable. |

The remaining release work is to merge this release-note finalization, tag the
exact resulting `main`, let the release workflow rebuild and publish its own
assets, and verify the public release metadata and checksums. Signing,
SmartScreen, manual Windows interactions, and native mobile claims retain the
boundaries above.
