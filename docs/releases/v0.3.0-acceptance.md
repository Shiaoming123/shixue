# v0.3.0 Acceptance Ledger

Updated: 2026-09-06 (Asia/Shanghai)

This ledger records evidence for the current v0.3.0 candidate only. Candidate
work began from `d048898`; no `v0.3.0` tag or GitHub Release exists. Results
from earlier PR4/local packages are historical and do not upgrade any v0.3.0
row.

Status meanings:

- `PASS`: the listed command or observation completed against this candidate.
- `FAIL`: the command ran but its acceptance condition did not pass.
- `NOT_RUN`: no result has been collected for this candidate.
- `BLOCKED`: required external identity, hosted release, device, or reputation
  evidence is unavailable.

## Machine gates

| Gate | Status | Evidence |
| --- | --- | --- |
| Version alignment (`package.json`, npm lockfile, Cargo manifest/lockfile, Tauri config) | `PASS` | All five version sources resolve to `0.3.0`; dependency crate versions are unchanged. |
| `npm run release:check` | `PASS` | Template-mode configuration check completed locally on 2026-09-06. |
| `npm run release:check -- --mode=release` | `PASS` | Strict release mode completed locally on 2026-09-06: 38 test files, 341 passed, 0 failed, 0 skipped; all 10 required command gates passed. Report: `src-tauri/target/release-check-report.json`. |
| `npm run check:protocol` | `PASS` | Application protocol validation completed locally on 2026-09-06. |
| `npm run check:docs` | `PASS` | Documentation link validation completed locally on 2026-09-06. |
| Focused release tests | `PASS` | Release-kit, Windows delivery, and Windows package smoke suites passed 42/42 on 2026-09-06. |
| `npm test` | `PASS` | 620/620 tests passed on 2026-09-06 with zero skipped tests. One prior run hit a Fetch-forbidden random loopback port; its isolated retry and the subsequent full run passed. |
| `npm run typecheck` | `PASS` | Executed by the strict release gate. |
| `npm run build` | `PASS` | Executed by the strict release gate; this is a frontend build, not a native package. |
| `npm run build:web` | `PASS` | Executed by the strict release gate; this proves a static Web build only. |
| Web smoke | `PASS` | `npm run smoke:web-persistence` passed 10 responsive viewports with 0 console/page errors; this remains separate from native installed-app evidence. |
| Calendar smoke | `PASS` | `npm run smoke:calendar` passed five viewports with 0 console/page errors. |
| Task-query benchmark | `PASS` | Node 24.14.1, Windows 10.0.22631, Ryzen 7 9850X3D, 16 logical CPUs: counts 3000/6000/41960; medians 7.9/7.5/134.4 ms after one untimed warmup and seven samples. |
| `npm run rust:verify` | `PASS` | Rust build/check and 11 Rust tests passed; no test was ignored. |
| `npm run release:provenance` | `NOT_RUN` | No `v0.3.0` tag exists, so tag/source provenance cannot yet pass. |
| v0.3.0 Windows package audit | `PASS` | `release-artifacts/windows/0.3.0/manifest.json` describes one NSIS, MSI, and Portable x64 artifact; size, header, SHA-256, and checksum-file audit passed. |

## Windows installed-app acceptance

The automated lifecycle used the exact NSIS bytes named by the v0.3.0 manifest.
No pre-existing HKCU product or uninstall identity was present before the run,
and no smoke directory or product registry key remained afterward.
The first attempt checked for executable removal immediately after NSIS returned
and failed the `silent-uninstall` stage while asynchronous self-removal was still
finishing. A bounded removal wait was added with a red/green test; rerunning the
same NSIS SHA-256 passed all six automated stages. The final rows below describe
that successful rerun, while the interaction rows remain unpromoted.

| Check | Status | Required evidence |
| --- | --- | --- |
| Produce Portable, NSIS, MSI, hashes, and manifest | `PASS` | `npm run package:windows`; unsigned x64 NSIS `71a047c493fee3205c83840d08a29ff981769b7ccfc58279a15154128e60478d`, MSI `a10f71435ddfa8533eecd06df25b3ff7f416abb73bc641f38377c1b39b8243f6`, Portable `997ce4b0cdaccbc58f6455ebeebf471cbe37e27631fb9fd4c8f4390589b5fb4f`. |
| Install the manifest-selected NSIS candidate | `PASS` | Silent installation completed in an isolated directory after the real identity preflight found no conflict. MSI installation remains `NOT_RUN`. |
| Launch and relaunch installed process | `PASS` | The exact installed executable survived two separate two-second probes. This is process evidence, not usable-window evidence. |
| Double-click launch and usable window | `NOT_RUN` | Manual installed-app observation; a Web smoke or process-liveness result is not accepted. |
| Retain local data across restart | `NOT_RUN` | Manual close/reopen observation with persisted study data. |
| Silent uninstall and cleanup | `PASS` | The uninstaller removed the executable after a bounded wait; smoke directory and product registry key were absent afterward. |
| User-visible uninstall and AppData behavior | `NOT_RUN` | Manual uninstall observation plus explicit retained/removed data behavior. |
| Global quick-add shortcut, four calendar views, and dark mode | `NOT_RUN` | Manual installed-app interaction. Browser smoke does not promote these native rows. |
| Tray hide, reopen, and quit lifecycle | `NOT_RUN` | Manual installed-app observation, including no delivery after quit. |
| Reminder permission, delivery, snooze, and completion | `NOT_RUN` | Manual installed-app observation for the documented reminder cases. Native notification action buttons remain `UNSUPPORTED`. |
| Windows display scaling at 200% | `NOT_RUN` | Manual installed-app observation. |

## Trust, updater, accessibility, and mobile

| Area | Status | Boundary |
| --- | --- | --- |
| Authenticode signing | `BLOCKED` | No certificate-backed publisher identity or signed v0.3.0 binaries are available. |
| Updater end-to-end | `BLOCKED` | Configuration exists, but there is no tagged, hosted, signed v0.3.0 updater payload and no installed-client update observation. |
| SmartScreen | `BLOCKED` | Requires the final downloaded artifact, valid publisher signing, and real reputation evidence. |
| Windows Narrator | `NOT_RUN` | Keyboard and screen-reader behavior requires manual validation in the installed app. |
| Android/iOS native acceptance | `NOT_RUN` | No v0.3.0 APK, AAB, IPA, simulator, or real-device run is recorded. Responsive Web evidence is not native evidence. |
| Mobile signing and store delivery | `BLOCKED` | Platform credentials, signed release artifacts, real-device checks, and store submission evidence are unavailable. |

The candidate must remain unreleased while any required core Windows installed-
app row is `NOT_RUN`. Signing, updater, and SmartScreen claims remain blocked
until their own external evidence exists.
